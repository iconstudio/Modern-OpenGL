module;
#include "Internal.hpp"
module Glib.Device.Resource.IBitmap;

using namespace gl::device::resource;

IBitmap IBitmap::Copy() const noexcept
{
	auto& handle = GetHandle();
	if (handle)
	{
	}


	return IBitmap();
}